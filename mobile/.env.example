# Local development: your laptop's LAN IP, not localhost.
# Find it with `ipconfig` (Windows) or `ifconfig | grep inet` (Mac/Linux).
EXPO_PUBLIC_API_URL=http://192.168.1.5:8000

# After you deploy the backend, switch to the public URL:
# EXPO_PUBLIC_API_URL=https://blindspot-api.onrender.com
